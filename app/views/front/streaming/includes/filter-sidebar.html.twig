                        <div class="py-8" id="filter-side" >
                            <p class="text-xl text-gray-400">{{lang.translate('Filters')}}</p>
                            <hr class="my-8 opacity-30" />
                            <ul class="dark:text-gray-400 flex lg:block">
                                <li class="relative show-child w-full">
                                    <div class="flex w-full">
                                        <span class="w-full text-base dark:text-gray-400  text-gray-600 ">{{lang.translate('Genre')}}</span>
                                        <img src="/src/front_assets/svg/arrow.svg" />
                                    </div>
                                    <div class="py-3 inline-block {{item.lang_content.title ?? 'hidden'}}">
                                        <span class="text-xs rounded-full py-1 px-2 bg-gray-300 text-gray-800  gap-2 inline-block">    
                                            <span class="px-2 flex gap-2">{{item.lang_content.title}} </span> 
                                        </span> 
                                    </div>
                                    <div class="show-on-hover absolute w-40 lg:top-[-30px] top-[30px] lg:left-[100%] dark:bg-gray-900 bg-white  z-10 p-8 space-y-3">
                                        {% for genre in genres %}
                                        <p><a href="/genre/{{genre.lang_content.prefix}}?params[type]={{app.params.type}}&params[date]={{app.params.date}}" class="ajax-load">{{genre.lang_content.title}}</a></p>
                                        {% endfor %}
                                    </div>
                                </li>
                                <li class="relative show-child w-full">
                                    <div class="flex w-full">
                                        <span class="w-full text-base dark:text-gray-400  text-gray-600 ">{{lang.translate('Type')}}</span>
                                        <img src="/src/front_assets/svg/arrow.svg" />
                                    </div>
                                    <div class="py-3 inline-block  ">
                                        <span class="{{app.params.type != '' ? '' : 'hidden'}} text-xs rounded-full py-1 px-2 bg-gray-300 text-gray-800  gap-2 inline-block">    
                                            <span class="px-2 flex gap-2">{{lang.translate(app.params.type)}} </span> 
                                        </span> 
                                    </div>
                                    <div class="show-on-hover absolute w-40 lg:top-[-30px] top-[30px] lg:left-[100%] dark:bg-gray-900 bg-white  z-10 p-8 space-y-3">
                                        {% for type in ['audio','audio_book','course'] %}
                                        <p><a href="/genre/{{item.lang_content.prefix}}?params[type]={{type}}&params[date]={{app.params.date}}" class="ajax-load">{{lang.translate(type)}}</a></p>
                                        {% endfor %}
                                    </div>
                                </li>
                                <li class="relative show-child w-full ">
                                    <div class="flex w-full">
                                        <span class="w-full text-base dark:text-gray-400  text-gray-600 ">{{lang.translate('Date')}}</span>
                                        <img src="/src/front_assets/svg/arrow.svg" />
                                    </div>
                                    <div class="py-2 inline-block ">
                                        <span class="text-xs rounded-full py-1 px-2 bg-gray-300 text-gray-800 {{app.params.date != '' ? '' : 'hidden'}}  gap-2 inline-block">    
                                            <span class="px-2 flex gap-2">{{lang.translate('last ' ~ app.params.date)}} </span> 
                                        </span> 
                                    </div>
                                    <div class="show-on-hover absolute w-40 lg:top-[-30px] top-[30px] lg:left-[100%] dark:bg-gray-900 bg-white  w-40 z-10 p-8 space-y-3">
                                        {% for period in ['day','week','month', 'year'] %}
                                        <p><a href="/genre/{{item.lang_content.prefix}}?params[type]={{app.params.type}}&params[date]={{period}}" class="ajax-load">{{lang.translate(period)}}</a></p>
                                        {% endfor %}
                                    </div>
                                </li>
                            </ul>
                        </div>