
                    <div class="w-full p-4 lg:p-8 bg-white dark:bg-gray-900 text-primary">

                        <h2 class="text-2xl font-normal dark:text-gray-400">{{lang.translate('Trnding Videos')}}</h2>
                        <hr class=" dark:opacity-30 my-6" />
                        
                        <div class="grid lg:grid-cols-4 md:grid-cols-3 sm:grid-cols-2 grid-cols-2 gap gap-4" x-data="{is_slide: ''}">
                            {% for media_item in video_items.items %}
                            {% set list = video_items %}
                            {% include 'views/front/'~ template ~ '/includes/video_item.html.twig' %}
                            {% endfor %}
                            
                        </div>
                    </div>