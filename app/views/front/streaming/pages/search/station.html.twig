
            
                

                <div class="w-full lg:flex gap-6 dark:bg-gray-900 bg-white   p-8 rounded-lg shadow shadow-lg relative">

                    <div class="w-full relative  rounded-lg shadow-md  ">
                        <div class="w-full text-primary border-1 p-8">
                            <div class="w-full dark:text-gray-400 text-xl font-bold tracking-wide">{{lang.translate('Similar to your search')}} {{app.params.title}}</div>
                        </div>
                        <hr class="opacity-30"/>
                        <div class="w-full p-8">
                            <div class="w-full flex">
                                <div class="w-full ">
                                    <ul class="w-full grid-cols-2 grid gap-10 pb-20">
                                        {% for item in search_list.items %}
                                        {% include 'views/front/' ~ template ~ '/includes/station-block.html.twig' %}
                                        {% endfor %}
                                    </ul>
                                </div>
                            </div>
                            {% include 'views/front/' ~ template ~ '/includes/pagination.html.twig' %}

                        </div>
                    </div>
                </div>

         