
                    <div class="w-full p-4 lg:p-8 bg-white dark:bg-gray-900 text-primary">

                        <h2 class="text-2xl font-normal dark:text-gray-400">{{lang.translate('Explore trends')}}</h2>
                        <hr class=" dark:opacity-30 my-2" />
                        
                        <div class="grid lg:grid-cols-4 md:grid-cols-3 sm:grid-cols-2 grid-cols-2 gap gap-4" x-data="{is_slide: 'w-full'}">
                        
                            {% for genre in genres %}
                                {% include 'views/front/' ~ template ~ '/includes/genres_list.html.twig' %}
                            {% endfor %}
                            
                        </div>
                    </div>