                
                <div class="p-4 w-full text-primary ">
                    
                    <div class="py-4 text-center">
                        <h1 class="px-6 text-2xl text-gray-800 dark:text-gray-100">{{item.name}} </h1>
                        <p class="px-6 text-primary">{{item.description}} </p>
                    </div>

                    <div id="channel-list"  class="w-full  " >
                        {% set customer = app.customer_auth() %}
                        {% for item in channels  %}
                        {% include 'views/front/' ~ template ~ '/includes/artist-block.html.twig' %}
                        {% endfor %}

                    </div>
                </div>
                <div class="h-10"></div>